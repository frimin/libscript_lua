cmake_minimum_required(VERSION 2.8)

FOREACH(MY_DEPENDENCIES
  lua
)

ADD_SUBDIRECTORY(${MY_DEPENDENCIES})
SET_PROJECT_OUTPUT_PROPERTIES(${MY_DEPENDENCIES})
SET_TARGET_PROPERTIES(${MY_DEPENDENCIES} PROPERTIES FOLDER "dependencies")

ENDFOREACH()